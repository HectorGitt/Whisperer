version: '3.8'

services:
  whisperer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whisperer
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    labels:
      - "com.whisperer.description=Horror story reader with CRT aesthetic"
      - "com.whisperer.version=1.0.0"
